<header class="navbar">
    <%-include('./walletModal.ejs') %>
    <div class="nav-inner">
        <div class="nav-left">
            <a href="/clientdashboard">
                <img src="static/public/images/Logo.png" alt="logo" class="nav-logo" />
            </a>
            <nav class="nav-links">
                <button class="nav-btn">Store</button>
                <button class="nav-btn">Auctions</button>
                <a class="nav-btn" href="/wishlistedauctions">Wishlist</a>

            </nav>
        </div>

<<<<<<< HEAD
        <div class="item-media">
          <button class="wishlist-heart" id="wishBtn" type="button" aria-label="Add to wishlist" title="Add to wishlist">
            ❤
          </button>

          <img src="<%= imgpath %>" alt="Auction item" id="itemImg" />
        </div>
      </div>

      <!-- Section 2 -->
      <div class="desc-card">
        <h3>Description</h3>
        <p id="itemDesc">
          <%= auction.item_desc %>
        </p>
      </div>
    </section>

    <!-- RIGHT -->
    <section class="auction-right">
      <!-- Section 3: Bidding Corner -->
      <div class="bid-card" id="bidCard">
        <button class="leave-btn" id="leaveBtn" type="button">Leave</button>

        <div class="current-label">Current bid</div>
        <div class="current-amount">$<span id="currentBid"><%= auction.highest_bid %></span></div>
<!-- (userBid ? userBid : auction.starting_price) -->
        <div class="highest-bidder">
          <span class="dot-avatar" id="topBidderIcon">
            <% if (auction.highest_bidder && auction.highest_bidder[0]) { %>
              <%= auction.highest_bidder[0].toUpperCase() %>
            <% } else { %>
              A
            <% } %>
          </span>

          <div class="hb-text">
            <div class="hb-label">Highest bidder</div>
            <div class="hb-name" id="topBidderName"><%= auction.highest_bidder || 'No bids yet' %></div>
          </div>
=======
        <div class="nav-center">
            <input id="search" class="search" type="search" placeholder="Search auctions, items or sellers..."
                aria-label="Search" />
>>>>>>> e66ea8cff8e74f8efd69e8fbb755d6e9852bf9d8
        </div>

        <div class="nav-right">
            <a href="/sellitem">
                <button class="btn sell">+ Sell</button>
            </a>

            <button class="balance" id="balanceBtn" type="button" aria-haspopup="dialog" aria-expanded="false">
                <img src="static/public/Icons/coin.png" alt="" class="balance-icon">
                $<span id="balance"><%= balance %></span>
            </button>

        <!-- expanded -->
        <form action="/AuctionDetails/bid" method="POST" class="bid-actions" id="bidActions">
            <input class="bid-input" id="bidInput" name="amount" type="number" step="any" inputmode="numeric" placeholder="Bid amount" aria-label="Bid amount" />
            <button class="bid-btn" id="bidBtn" type="submit">Bid</button>
          <button class="buyout-btn" id="buyoutBtn" type="button" data-buyout="<%-"$"+auction.buy_out_price %>">
            Buyout
          </button>
        </form>
      </div>

                    </div>
                    <hr class="pd-sep" />
                    <div class="pd-row pd-row--actions">
                        <a href="/manageprofile">
                        <button class="pd-btn">Manage Profile</button>
                        </a>
                        <form action="/myauctions" method="POST">
                            <button class="pd-btn" type="submit">My Auctions</button>
                        </form>
                        <button class="pd-btn">Sold items</button>
                        <a href="/wishlistedauctions">
                            <button class="pd-btn">Wishlist</button>
                        </a>
                        <button class="pd-btn">My wallet</button>
                    </div>
                    <hr class="pd-sep" />
                    <div class="pd-row pd-row--logout">
                        <a href="/signup">
                            <button class="pd-btn pd-logout">Log out</button>
                        </a>
                    </div>
                </div>
            </div>
        </div>
<<<<<<< HEAD
      </div>
    </section>
  </main>
  <script>
    const bidCard = document.getElementById("bidCard");
    const getToBidBtn = document.getElementById("getToBidBtn");
    const leaveBtn = document.getElementById("leaveBtn");
    const bidActions = document.getElementById("bidActions");
    const yourBidRow = document.getElementById("yourBidRow");
    let joined = false;

    getToBidBtn.addEventListener("click", () => {
      if (joined) return;
      joined = true;

      bidCard.classList.add("joined");

      // // Suggest minimum next bid = current + 5%
      // const base = currentBidNumber();
      // const minNext = Math.ceil(base + base * 0.05);
      // bidInput.value = String(minNext);
    });
    leaveBtn.addEventListener("click", () => {
      joined = false;
      bidCard.classList.remove("joined");
      if (yourBidRow) yourBidRow.style.display = "none";
    });

    function pad2(n) { 
      return String(n).padStart(2, '0'); 
    }

    function formatRemaining(ms) {
      if (ms <= 0) return "ENDED";
      const totalSec = Math.floor(ms / 1000);
      const h = Math.floor(totalSec / 3600);
      const m = Math.floor((totalSec % 3600) / 60);
      const s = totalSec % 60;
      return `${pad2(h)}:${pad2(m)}:${pad2(s)}`;
    }

    function tickTimers() {
      const now = Date.now();
      document.querySelectorAll('.js-timer').forEach(el => {
        const endStr = el.getAttribute('data-end');
        const end = new Date(endStr).getTime();
        el.textContent = formatRemaining(end - now);
      });
    }

    tickTimers();
    setInterval(tickTimers, 1000);
  </script>
</body>

</html>
=======
</header>
<script>
    (function () {
        const avatar = document.querySelector('.profile .avatar');
        const dropdown = document.querySelector('.profile .profile-dropdown');
        if (!avatar || !dropdown) return;

        function positionDropdown() {
            const rect = avatar.getBoundingClientRect();
            const pad = 8;
            // place dropdown under avatar, aligned to avatar's right edge
            const top = rect.bottom + pad + window.scrollY;
            let left = rect.right - dropdown.offsetWidth + window.scrollX;
            // keep within viewport
            if (left < 8) left = 8;
            if (left + dropdown.offsetWidth > window.innerWidth - 8) {
                left = Math.max(8, window.innerWidth - dropdown.offsetWidth - 8);
            }
            dropdown.style.top = top + 'px';
            dropdown.style.left = left + 'px';
        }

        document.addEventListener('click', function (e) {
            // Toggle when clicking avatar
            if (avatar.contains(e.target)) {
                const open = dropdown.classList.toggle('show');
                avatar.setAttribute('aria-expanded', open ? 'true' : 'false');
                dropdown.setAttribute('aria-hidden', open ? 'false' : 'true');
                if (open) {
                    // compute position after it's visible to get correct width
                    dropdown.style.display = 'block';
                    // small timeout to ensure styles/layout applied
                    requestAnimationFrame(positionDropdown);
                } else {
                    dropdown.style.display = 'none';
                }
                return;
            }

            // Close when clicking outside
            if (!dropdown.contains(e.target)) {
                dropdown.classList.remove('show');
                avatar.setAttribute('aria-expanded', 'false');
                dropdown.setAttribute('aria-hidden', 'true');
                dropdown.style.display = 'none';
            }
        });

        // Close on Escape
        document.addEventListener('keydown', function (e) {
            if (e.key === 'Escape') {
                dropdown.classList.remove('show');
                avatar.setAttribute('aria-expanded', 'false');
                dropdown.setAttribute('aria-hidden', 'true');
                dropdown.style.display = 'none';
            }
        });

        // Reposition on resize/scroll while open
        window.addEventListener('resize', () => { if (dropdown.classList.contains('show')) positionDropdown(); });
        window.addEventListener('scroll', () => { if (dropdown.classList.contains('show')) positionDropdown(); });
    })();
</script>
<script>
  (function(){
    const btn = document.getElementById('balanceBtn');
    const modal = document.getElementById('walletModal');
    if (!btn || !modal) return;

    function openModal(){
      modal.classList.add('is-open');
      modal.setAttribute('aria-hidden', 'false');
      btn.setAttribute('aria-expanded', 'true');
      document.body.style.overflow = 'hidden';
    }

    function closeModal(){
      modal.classList.remove('is-open');
      modal.setAttribute('aria-hidden', 'true');
      btn.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = '';
    }

    btn.addEventListener('click', openModal);

    modal.addEventListener('click', (e) => {
      if (e.target.matches('[data-close="wallet"]')) closeModal();
    });

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && modal.classList.contains('is-open')) closeModal();
    });
  })();
</script>
>>>>>>> e66ea8cff8e74f8efd69e8fbb755d6e9852bf9d8
